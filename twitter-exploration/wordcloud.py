# import json
#
# from wordcloud import WordCloud
#
# file = open("tweets.json", "r")
# tweetData = json.load(file)
# file.close()
#
# strings = ""
#
# for tweets in tweetData:
#     strings += tweets["text"]
#     textBlob = TextBlob(tweets["text"])
#     strings.append(textBlob.)
# #     filteredWords[word] = count
# #     if continue
# # print(tweets)
